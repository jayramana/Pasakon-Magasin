import React, { useEffect } from 'react';
import { useData, Laptop } from '../hooks/GlobalDataProvider'

const App = () => {
    const { dispatch } = useData();


    return (
        <div>
            
        </div>
    );
};

export default App;
